import Quick
import Nimble
import GPXKit

class CommonSpec: QuickSpec {
    override func spec() {
        describe("common data") {
            pending("should have a creator name") {}
            
            context("metadata") {
                pending("should have a name") {}
                pending("should have a description") {}
                pending("should have a author") {}
                pending("should have a copyright notice") {}
                pending("should have a link") {}
                pending("should have a time") {}
                pending("should have keywords") {}
                pending("should have bounds") {}
            }
        }
    }
}
