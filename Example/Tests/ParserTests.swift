import Quick
import Nimble
import GPXKit

class ParserSpec: QuickSpec {
    override func spec() {
        describe("parser") {
            pending("should be successful") {}
            pending("should throw an parse error") {}
            pending("should throw an empty error") {}
        }
    }
}
